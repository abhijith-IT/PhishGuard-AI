name: phishguard-ai

services:
  backend:
    build:
      context: ./backend
    container_name: phishguard-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    container_name: phishguard-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped
